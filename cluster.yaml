apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: smaitic-cluster
  region: ap-south-1
  version: "1.33"

managedNodeGroups:
  - name: worker-node
    instanceType: t3.small
    desiredCapacity: 1
    minSize: 1
    maxSize: 1
    volumeSize: 20